/**
 * Next.js Instrumentation hook.
 * Migration is triggered manually from the "Run Migration" button in the UI header.
 */
export async function register() {
  // no-op — migration is not run on startup
}
